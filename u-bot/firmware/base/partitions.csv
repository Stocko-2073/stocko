# U-BOT base, 4 MB flash, two OTA slots and no factory app.
#
# The bootloader's rollback support (CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE)
# needs otadata; without a factory slot a failed image falls back to the other
# OTA slot, which is the one that was running before the update.
#
# Name,     Type, SubType,  Offset,    Size,     Flags
nvs,        data, nvs,      0x9000,    0x6000,
otadata,    data, ota,      0xf000,    0x2000,
phy_init,   data, phy,      0x11000,   0x1000,
ota_0,      app,  ota_0,    0x20000,   0x1E0000,
ota_1,      app,  ota_1,    0x200000,  0x1E0000,
